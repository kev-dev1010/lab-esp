mode: restricted
description: configuracao minima de isolamento para automacao com IA
filesystem:
  writable_paths:
    - .
network:
  policy: deny
commands:
  allowlist_file: sandbox/ai-jail/allowlist.example.txt
