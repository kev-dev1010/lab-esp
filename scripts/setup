#!/usr/bin/env bash
set -euo pipefail
source "$(dirname "$0")/_lib.sh"

log "Preparando ambiente do projeto"
if [ ! -f ".env" ] && [ -f ".env.example" ]; then
  cp .env.example .env
  log "Arquivo .env criado a partir de .env.example"
fi

npm install
log "Setup finalizado"
